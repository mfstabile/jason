/* Jason Project

   See Prometheus specification in doc folder

*/

MAS domestic_robot {
    infrastructure: Centralised(anytime_asynch)
    environment: HouseEnv(gui) // use "nogui" as parameter to not show the GUI
    agents: robot;
            owner;
            supermarket;
}

